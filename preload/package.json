{
    "name": "@betterdiscord/preload",
    "version": "0.0.1",
    "description": "BetterDiscord preload module",
    "main": "src/index.js",
    "private": true,
    "scripts": {
        "build": "pnpm webpack --progress --color",
        "watch": "pnpm webpack --progress --color --watch",
        "build-prod": "pnpm webpack --stats minimal --mode production",
        "lint": "pnpm eslint .",
        "lint-fix": "pnpm run lint --fix",
        "lint-prod": "pnpm run lint --quiet"
    },
    "devDependencies": {
        "webpack": "^5.90.3"
    }
}
